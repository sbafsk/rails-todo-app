<h1>To Do: </h1>

<ul>
<% @task.each do |t| %>
  <li>
    <h3><strong><%= link_to t.title, task_path(t) %></strong></h3>    
    <p>Completed:<%= t.completed.to_s %></p>
  </li>
<% end %>
</ul>

<%= link_to 'New task', new_task_path %>